apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}
data:
  ENVIRONMENT: {{ .Values.config.environment | default "production" }}
  API_BASE_URL: {{ .Values.config.apiBaseUrl | default "http://swishops-backend-svc" }}
